/**
 * @format
 */

import { AppRegistry } from 'react-native';
import App from './App';

// Must match `withModuleName` in ios/Atlet/AppDelegate.swift.
AppRegistry.registerComponent('Atlet', () => App);
